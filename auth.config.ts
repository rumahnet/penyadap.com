/* Removed NextAuth provider config. Supabase is used for authentication.
   If you need a provider-based sign-in flow, wire it through Supabase or
   implement the needed adapters in `components/providers/supabase-provider.tsx`.
*/

export default {};

